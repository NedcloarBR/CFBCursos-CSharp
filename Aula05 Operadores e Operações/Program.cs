using System;

namespace Aula05
{
    class Program
    {
        static void Main(string[] args)
        {
            bool res = 10>5; // > < >= <= !=

            //% & = AND / E
            //$ | = OR / OU
            bool ReS = (5>3) & (10>5);

            int num = 10;


            // num++; // num+=1; // num=num+1;
            // num+=2; // num=num+2; // + - * /

            Console.WriteLine(num);

            Console.WriteLine(res);

            Console.WriteLine(ReS);
        }
    }
}
